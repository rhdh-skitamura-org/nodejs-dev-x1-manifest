apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: nodejs-dev-x1-listener
spec:
  serviceAccountName: trigger-sa 
  triggers:
    - bindings:
        - ref: nodejs-dev-x1-trigger-binding
      template:
        ref: nodejs-dev-x1-pipeline-template
      interceptors:
      - ref:
          name: github
          kind: ClusterInterceptor
        params:
        - name: "secretRef"
          value:
            secretName: webhooksecret
            secretKey: webhook_secret
        - name: "eventTypes"
          value: ["push"]
